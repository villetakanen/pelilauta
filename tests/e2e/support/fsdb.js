const seed = () => {
  console.log('Seeding the Firestore Database')
  /* const db = firebase.firestore()
  const e2eSiteRef = db.collection('sites').doc('e2e-testing')
  e2eSiteRef.get().then((siteDoc) => {
    console.log('fsdb got siteref', siteDoc.id)
  }) */
}

exports.seed = seed